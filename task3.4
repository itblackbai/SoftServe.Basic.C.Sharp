            // Enter a sequence of positive integers (to the first negative).
            //  Calculate the arithmetic average of the entered  positive numbers.
            int sum = 0;
            for (int i = 0; i < 100; i++)
            {
                Console.WriteLine("Enter possitive number ");
                int posit_number = int.Parse(Console.ReadLine());
                if (posit_number >= 0)
                {
                    sum += posit_number;
                    Console.WriteLine("You enter possitive number");
                    Console.WriteLine($"Sum is {sum}") ;
                }
                else 
                {
                    Console.WriteLine("Please enter possitive number");
                }
            }


            Console.ReadLine();
